{
  "name": "@regattadesk/frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "echo 'Lint placeholder - to be configured later'",
    "test": "echo 'Unit tests not yet configured' && exit 0",
    "test:a11y": "echo 'Accessibility tests not yet configured' && exit 0"
  },
  "dependencies": {
    "vue": "3.5.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "6.0.2",
    "vite": "7.3.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
