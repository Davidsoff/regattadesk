# Development Profile Configuration
# This profile is active when QUARKUS_PROFILE=dev or running with quarkus:dev

# Database Configuration (Development)
# Uses weak credentials for local development convenience
# DO NOT USE IN PRODUCTION
quarkus.datasource.username=${DB_USER:regattadesk}
quarkus.datasource.password=${DB_PASSWORD:regattadesk}
quarkus.datasource.jdbc.url=${DB_URL:jdbc:postgresql://localhost:5432/regattadesk}

# Enable Quarkus Dev Services for PostgreSQL (optional, starts Testcontainer if no DB_URL set)
# quarkus.datasource.devservices.enabled=true
